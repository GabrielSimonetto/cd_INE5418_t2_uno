# O INCRIVEL UNO ESCRITO EM JGROUPS

## Para rodar:

Na sua primeira vez compilando use `make build`

Se voce quiser recompilar use `make all` (make clean + make build)

Se voce quiser rodar algo ja compilado, use `make run`
